class AlgorithmManager {
  create(algorithm, array) {
    // Return a new instance of given algorithm with given array
    switch (algorithm) {
      case "sort1":
        break;
      case "sort2":
        break;

      default:
        return new Error("Unknown Algorithm");
    }
  }
}
